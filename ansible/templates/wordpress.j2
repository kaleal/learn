upstream wordpress {
   {% for host in groups['webservers'] %}
   server {{host}};
   {% endfor %}
}

server {
   listen 80;

   server_name www.bla.com.br;
   location /wordpress {
      proxy_pass http://wordpress;
   }
}
